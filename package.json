{
  "dependencies": {
    "bootstrap": "^5.3.7"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "sass": "^1.89.2"
  },
    "scripts": {
    "dev": "browser-sync start --proxy 'localhost/ivs' --files 'assets/css/*.css', 'assets/js/*.js', '*.php', 'templates/*.php', 'user/*.php' --watch",
    "sass": "sass --watch assets/scss:assets/css"
  }
}
